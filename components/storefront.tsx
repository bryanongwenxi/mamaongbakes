'use client';
import { useState,useEffect,useRef, type ReactNode } from 'react';
import { ArrowRight,ArrowUpRight,ShoppingBag,Plus,Minus,X,MapPin,Truck,Heart,Camera,Check,Search,ChevronDown,MessageCircle,Copy,Flower2,Menu } from 'lucide-react';
import { BasketItem,Product,Variant,OrderDetails,categories,money,priceOf } from '@/lib/types';
import { resolveBasket,orderMessage,whatsappLink,singaporeToday } from '@/lib/order';
export function Brand(){return <a className="brand" href="/" aria-label="Mama Ong Bakes home"><span className="brand-flower"><Flower2 strokeWidth={1.2}/></span><span>mama ong<span className="brand-small">B A K E S</span></span></a>;}
export function Modal({title,children,onClose,wide=false}:{title:string;children:ReactNode;onClose:()=>void;wide?:boolean}){const ref=useRef<HTMLDialogElement>(null);useEffect(()=>{const dialog=ref.current;const previous=document.activeElement as HTMLElement;dialog?.showModal();const overflow=document.body.style.overflow;document.body.style.overflow='hidden';return()=>{dialog?.close();document.body.style.overflow=overflow;previous?.focus();};},[]);return <dialog ref={ref} className={`modal ${wide?'wide':''}`} onCancel={onClose} onClick={e=>{if(e.target===ref.current){const r=ref.current.getBoundingClientRect();if(e.clientX<r.left||e.clientX>r.right||e.clientY<r.top||e.clientY>r.bottom)onClose();}}} aria-label={title}><button className="icon-button close" onClick={onClose} aria-label="Close"><X size={22}/></button>{children}</dialog>;}
function Price({variant}:{variant:Variant}){return <span className="price">{variant.salePrice!==null&&<del>{money(variant.price)}</del>}<span>{money(priceOf(variant))}</span></span>;}
function Photo({product,className=''}:{product:Product;className?:string}){return <img className={className} src={product.image} alt={product.name} loading="lazy" onError={e=>{e.currentTarget.style.objectFit='contain';e.currentTarget.src='/icon.svg';}}/>;}
export default function Storefront({initialProducts}:{initialProducts:Product[]}){
 const [products,setProducts]=useState(initialProducts),[category,setCategory]=useState('All bakes'),[query,setQuery]=useState(''),[basket,setBasket]=useState<BasketItem[]>([]),[hydrated,setHydrated]=useState(false),[selected,setSelected]=useState<Product|null>(null),[showBasket,setShowBasket]=useState(false),[toast,setToast]=useState(''),[mobileNav,setMobileNav]=useState(false);
 useEffect(()=>{try{const saved=JSON.parse(localStorage.getItem('mama-basket-v1')??'[]');if(Array.isArray(saved))setBasket(saved.filter(v=>typeof v?.productId==='string'&&typeof v.variantId==='string'&&Number.isInteger(v.quantity)&&v.quantity>0&&v.quantity<=99).slice(0,150));}catch{}setHydrated(true);},[]);
 useEffect(()=>{if(hydrated)try{localStorage.setItem('mama-basket-v1',JSON.stringify(basket));}catch{}},[basket,hydrated]);
 useEffect(()=>{if(toast){const timer=setTimeout(()=>setToast(''),3000);return()=>clearTimeout(timer);}},[toast]);
 const lines=resolveBasket(basket,products);const count=lines.reduce((s,i)=>s+i.quantity,0);const subtotal=lines.reduce((s,i)=>s+i.total,0);
 function add(product:Product,variant:Variant,quantity=1){setBasket(prev=>{const current=prev.find(i=>i.productId===product.id&&i.variantId===variant.id);return current?prev.map(i=>i===current?{...i,quantity:Math.min(99,i.quantity+quantity)}:i):[...prev,{productId:product.id,variantId:variant.id,quantity}];});setToast(`${product.name} added to your basket`);}
 const visible=products.filter(p=>(category==='All bakes'||p.category===category)&&`${p.name} ${p.description}`.toLowerCase().includes(query.toLowerCase()));
 const hero=products.find(p=>p.id==='kueh-salat')??products[0];
 return <><a className="skip-link" href="#menu">Skip to the menu</a><div className="announcement"><span><MapPin size={14}/> Free self-collection at Pasir Ris</span><span className="announcement-divider">✦</span><span>Delivery fee confirmed with your order</span></div><header className="header"><div className="nav-wrap"><Brand/><nav className={mobileNav?'nav-links open':'nav-links'} aria-label="Main navigation"><a href="#menu" onClick={()=>setMobileNav(false)}>Our bakes</a><a href="#story" onClick={()=>setMobileNav(false)}>Mama’s story</a><a href="#how-to-order" onClick={()=>setMobileNav(false)}>How to order</a></nav><div className="nav-actions"><button className="basket-button" onClick={()=>setShowBasket(true)}><ShoppingBag size={18}/><span>Basket</span><b>{count}</b></button><button className="icon-button mobile-toggle" onClick={()=>setMobileNav(!mobileNav)} aria-label="Toggle menu" aria-expanded={mobileNav}>{mobileNav?<X/>:<Menu/>}</button></div></div></header>
 <main><section className="hero wrap"><div className="hero-copy"><div className="eyebrow"><span className="small-line"/> A HOME KITCHEN IN SINGAPORE</div><h1>A little taste<br/>of <em>home.</em><svg className="hero-spark" viewBox="0 0 60 60" aria-hidden="true"><path d="M30 3v18M30 39v18M3 30h18M39 30h18M11 11l12 12M39 39l10 10M11 49l12-12M39 21l10-10"/></svg></h1><p>Familiar favourites. A few new loves.<br/>All made by Mama, for your table.</p><a className="button" href="#menu">Find your favourites <ArrowRight size={18}/></a><div className="hero-footnote"><Heart size={17} strokeWidth={1.3}/><span>Made from scratch. Shared with love.</span></div></div><div className="hero-visual"><div className="hero-photo">{hero&&<img src={hero.image} alt="Mama Ong’s homemade pandan kueh salat with butterfly pea glutinous rice" fetchPriority="high"/>}</div><div className="homemade-seal"><span>FROM MAMA’S</span><Heart size={28} strokeWidth={1}/><span>HOME KITCHEN</span></div><div className="photo-caption"><span>Something sweet, something familiar.</span><span>01 / MAMA’S KUEH</span></div></div></section>
 <section className="values-strip" aria-label="Our kitchen"><span>Made by hand <Flower2/></span><span>Baked with care <Flower2/></span><span>For the people you love <Flower2/></span><span>A little more home <Flower2/></span></section>
 <section className="menu-section wrap" id="menu"><div className="section-top"><div><p className="eyebrow">FROM OUR KITCHEN</p><h2>What sounds good?</h2></div><p>A treat for yourself, a box to share. <br/>There’s a little something for every table.</p></div><div className="menu-toolbar"><div className="category-tabs" role="group" aria-label="Filter bakes">{categories.map(c=><button key={c} className={category===c?'active':''} aria-pressed={category===c} onClick={()=>setCategory(c)}>{c}</button>)}</div><label className="search"><Search size={17}/><input aria-label="Search bakes" placeholder="Find a favourite" value={query} onChange={e=>setQuery(e.target.value)}/>{query&&<button onClick={()=>setQuery('')} className="icon-button" aria-label="Clear search"><X size={14}/></button>}</label></div><div className="menu-meta"><span>{visible.length} homemade favourites</span><span>All prices in SGD · Made to order</span></div>
 {visible.length?<div className="product-grid">{visible.map(product=><ProductCard key={product.id} product={product} onDetails={()=>setSelected(product)} onAdd={v=>add(product,v)}/>)}</div>:<div className="empty-results"><Flower2/><h3>No bakes found.</h3><p>Try another search, or take a look at the whole menu.</p><button className="text-button" onClick={()=>{setQuery('');setCategory('All bakes');}}>See all bakes <ArrowRight size={16}/></button></div>}
 <div className="menu-note"><Heart size={20} strokeWidth={1.3}/><p>A little note from our kitchen: everything is made to order. We’ll confirm availability and your preferred date together on WhatsApp.</p></div></section>
 <section className="story-section" id="story"><div className="story-inner wrap"><div className="story-photo"><img src="/images/cinnamonroll.jpg" alt="Cinnamon rolls from Mama Ong’s kitchen" loading="lazy"/><span className="handwritten">Always room for one more.</span></div><div className="story-copy"><p className="eyebrow">MEET THE HANDS BEHIND THE BAKES</p><h2>From Mama’s kitchen,<br/>with love.</h2><p>It started in our home kitchen in Singapore. A love for baking, something good in the oven, and the simple happiness of feeding the people around us.</p><p>Today, Mama still makes each order with that same care. From the kueh you grew up with to a loaf for tomorrow’s breakfast, these are the things we love having on our own table.</p><p className="story-signature">We’d love to share them with you.</p><a className="text-button" href="https://www.instagram.com/mamaongbakes/" target="_blank" rel="noreferrer"><Camera size={17}/> A peek into our kitchen <ArrowUpRight size={17}/></a></div></div></section>
 <section className="how-section wrap" id="how-to-order"><div className="section-top"><div><p className="eyebrow">A FEW SIMPLE STEPS</p><h2>Let’s make something lovely.</h2></div><p>Small home kitchen. A real person to talk to. <br/>We’ll work out the little details together.</p></div><div className="steps"><article><span className="step-number">01</span><ShoppingBag strokeWidth={1.2}/><h3>Pick your favourites</h3><p>Choose your bakes, sizes and quantities. Your basket keeps everything in one place.</p></article><article><span className="step-number">02</span><MessageCircle strokeWidth={1.2}/><h3>Have a little chat</h3><p>Send your basket to us on WhatsApp. We’ll confirm the date, availability and final total, then arrange payment in chat.</p></article><article><span className="step-number">03</span><Heart strokeWidth={1.2}/><h3>Bring home the good stuff</h3><p>Collect for free in Pasir Ris, or arrange delivery. We’ll confirm the delivery fee before you pay.</p></article></div><div className="collection-panel"><div><MapPin size={27} strokeWidth={1.3}/><div><h3>Self-collection at Pasir Ris</h3><p>Always free. We’ll share the exact location and timing in chat.</p></div><span className="pill">ON THE HOUSE</span></div><div><Truck size={27} strokeWidth={1.3}/><div><h3>A little further from home?</h3><p>Delivery is available by arrangement. Fee confirmed with your order.</p></div></div></div></section>
 <section className="faq-section wrap"><div><p className="eyebrow">GOOD TO KNOW</p><h2>A few little details.</h2></div><div className="faqs">{[['Is my order confirmed when I send my basket?','Your basket is an order enquiry. We’ll check what Mama can make, agree on a collection or delivery date, and confirm your order in WhatsApp.'],['How do I make payment?','Once we’ve confirmed availability and the final amount (including delivery, if needed), we’ll arrange payment with you directly in chat. Please wait for our confirmation before paying.'],['Can I ask about ingredients or allergies?','Of course. Open any bake for ingredient notes, and tell us about any dietary needs in your order message. Our ingredient notes are currently estimates awaiting Mama’s confirmation. Please check with us before ordering if you have an allergy.'],['When should I place my order?','Choose your preferred date in the basket and we’ll check it with Mama. As a home kitchen, we can only take on the orders we have time to make with care.']].map(([q,a])=><details key={q}><summary>{q}<Plus size={19}/></summary><p>{a}</p></details>)}</div></section>
 <section className="closing"><Flower2 strokeWidth={1}/><p className="eyebrow">THERE’S ALWAYS SOMETHING GOOD IN THE KITCHEN</p><h2>Come hungry.<br/>Leave with a little happiness.</h2><a href="#menu" className="button">Explore Mama’s bakes <ArrowRight size={18}/></a></section></main>
 <footer className="footer"><div className="wrap footer-top"><Brand/><p>Homemade in Singapore.<br/>Shared with love, one order at a time.</p><div><a href="https://www.instagram.com/mamaongbakes/" target="_blank" rel="noreferrer">Instagram <ArrowUpRight size={15}/></a><a href="https://wa.me/6592301768" target="_blank" rel="noreferrer">Say hello on WhatsApp <ArrowUpRight size={15}/></a></div></div><div className="wrap footer-bottom"><span>© {new Date().getFullYear()} Mama Ong Bakes</span><span>From our kitchen, to your table.</span><a href="/admin">Mama’s kitchen login</a></div></footer>
 {count>0&&!showBasket&&<button className="mobile-basket" onClick={()=>setShowBasket(true)}><span><ShoppingBag size={18}/> Your basket · {count}</span><span>{money(subtotal)} <ArrowRight size={18}/></span></button>}
 <div className={`toast ${toast?'visible':''}`} role="status">{toast&&<><Check size={18}/>{toast}<button onClick={()=>{setSelected(null);setShowBasket(true);setToast('');}}>View basket</button></>}</div>
 {selected&&<ProductDetails product={selected} onClose={()=>setSelected(null)} onAdd={(v,q)=>{add(selected,v,q);setSelected(null);}}/>}
 {showBasket&&<Basket basket={basket} products={products} setProducts={setProducts} setBasket={setBasket} onClose={()=>setShowBasket(false)}/>}
 </>;
}
function ProductCard({product,onDetails,onAdd}:{product:Product;onDetails:()=>void;onAdd:(v:Variant)=>void}){const [variantId,setVariantId]=useState(product.variants[0].id);const variant=product.variants.find(v=>v.id===variantId)??product.variants[0];return <article className={`product-card ${!product.available?'unavailable':''}`}><button className="product-image" onClick={onDetails} aria-label={`View ${product.name} details`}><Photo product={product}/>{!product.available?<span className="product-tag">Taking a little break</span>:variant.salePrice!==null?<span className="product-tag offer">A little treat · special price</span>:product.featured&&<span className="product-tag">From Mama’s favourites</span>}<span className="image-details">A closer look <ArrowUpRight size={16}/></span></button><div className="product-heading"><h3><button onClick={onDetails}>{product.name}</button></h3><Price variant={variant}/></div><p className="product-description">{product.description}</p><button className="ingredient-link" onClick={onDetails}>Ingredients & details <ArrowUpRight size={13}/></button><div className="product-bottom"><label className="variant-select"><span className="sr-only">Size for {product.name}</span><select value={variant.id} onChange={e=>setVariantId(e.target.value)}>{product.variants.map(v=><option key={v.id} value={v.id}>{v.label}</option>)}</select><ChevronDown size={14}/></label><button className="add-button" disabled={!product.available} onClick={()=>onAdd(variant)} aria-label={`Add ${product.name} to basket`}><Plus size={18}/><span>Add</span></button></div></article>;}
function ProductDetails({product,onClose,onAdd}:{product:Product;onClose:()=>void;onAdd:(v:Variant,q:number)=>void}){const [id,setId]=useState(product.variants[0].id),[quantity,setQuantity]=useState(1);const variant=product.variants.find(v=>v.id===id)!;return <Modal title={product.name} onClose={onClose} wide><div className="detail-grid"><Photo product={product} className="detail-photo"/><div className="detail-copy"><p className="eyebrow">{product.category}</p><h2>{product.name}</h2><p>{product.description}</p><label className="field">Choose your size<select value={id} onChange={e=>setId(e.target.value)}>{product.variants.map(v=><option key={v.id} value={v.id}>{v.label} · {money(priceOf(v))}</option>)}</select></label><div className="detail-price"><Price variant={variant}/><span>per {variant.label}</span></div><div className="ingredient-box"><h4>{product.ingredientsVerified?'Ingredients':'Likely ingredients · not yet verified'}</h4><p>{product.ingredients||'Please ask us for ingredients in chat.'}</p><h4>{product.ingredientsVerified?'Allergens':'Possible allergens · please confirm'}</h4><p>{product.allergens||'Please check with us before ordering.'}</p>{!product.ingredientsVerified&&<small>These notes are estimates. Please confirm ingredients and any allergy concerns with Mama before ordering.</small>}</div><div className="detail-actions"><Quantity value={quantity} name={product.name} onChange={setQuantity} min={1}/><button className="button" disabled={!product.available} onClick={()=>onAdd(variant,quantity)}>{product.available?'Add to basket':'Currently unavailable'}<Plus size={17}/></button></div><small>Made to order. Availability confirmed in WhatsApp.</small></div></div></Modal>;}
function Quantity({value,name,onChange,min=0}:{value:number;name:string;onChange:(n:number)=>void;min?:number}){return <div className="quantity"><button disabled={value<=min} onClick={()=>onChange(Math.max(min,value-1))} aria-label={`Decrease ${name} quantity`}><Minus size={14}/></button><span aria-label={`${name} quantity`}>{value}</span><button disabled={value>=99} onClick={()=>onChange(Math.min(99,value+1))} aria-label={`Increase ${name} quantity`}><Plus size={14}/></button></div>;}
function Basket({basket,products,setProducts,setBasket,onClose}:{basket:BasketItem[];products:Product[];setProducts:(p:Product[])=>void;setBasket:React.Dispatch<React.SetStateAction<BasketItem[]>>;onClose:()=>void}){
 const [details,setDetails]=useState<OrderDetails>({name:'',method:'collection',date:'',address:'',notes:''}),[review,setReview]=useState(false),[busy,setBusy]=useState(false),[error,setError]=useState(''),[copied,setCopied]=useState(false),[changed,setChanged]=useState(false);
 const lines=resolveBasket(basket,products);const subtotal=lines.reduce((s,i)=>s+i.total,0);const message=orderMessage(basket,products,details);const unavailable=basket.length-lines.length;
 function update(productId:string,variantId:string,quantity:number){setReview(false);setBasket(prev=>prev.map(i=>i.productId===productId&&i.variantId===variantId?{...i,quantity}:i).filter(i=>i.quantity>0));}
 async function prepare(e:React.FormEvent){e.preventDefault();setError('');setBusy(true);try{if(details.date<singaporeToday())throw Error('Please choose today or a future date.');const response=await fetch('/api/catalog',{cache:'no-store'});if(!response.ok)throw Error('We couldn’t check the latest menu. Please try again.');const data=await response.json();const before=JSON.stringify(lines.map(i=>[i.product.name,i.variant.label,i.quantity,priceOf(i.variant)]));const after=JSON.stringify(resolveBasket(basket,data.products).map(i=>[i.product.name,i.variant.label,i.quantity,priceOf(i.variant)]));setProducts(data.products);setChanged(before!==after);if(before!==after){setError('The menu has changed. Please review your basket and subtotal, then prepare your message again.');return;}if(!lines.length)throw Error('Add a bake to your basket first.');setReview(true);}catch(e){setError((e as Error).message);}finally{setBusy(false);}}
 return <Modal title="Your basket" onClose={onClose}><div className="basket-content"><p className="eyebrow">A LITTLE BOX OF HAPPINESS</p><h2>Your basket<span className="basket-flower"><Flower2 strokeWidth={1}/></span></h2>{!lines.length?<div className="empty-basket"><ShoppingBag size={44} strokeWidth={1}/><h3>Something lovely belongs here.</h3><p>Pick a few favourites and we’ll take it from there.</p><button className="button" onClick={onClose}>Explore the bakes <ArrowRight size={17}/></button></div>:<><p className="basket-intro">An order enquiry, made easy. Mama will confirm the little details with you in chat.</p><div className="basket-lines">{lines.map(i=><div className="basket-line" key={`${i.productId}-${i.variantId}`}><Photo product={i.product}/><div><h4>{i.product.name}</h4><p>{i.variant.label}</p><Quantity value={i.quantity} name={i.product.name} onChange={q=>update(i.productId,i.variantId,q)}/></div><div className="line-end"><strong>{money(i.total)}</strong><button className="remove-button" onClick={()=>update(i.productId,i.variantId,0)} aria-label={`Remove ${i.product.name}`}>Remove</button></div></div>)}</div>{unavailable>0&&<p className="notice">{unavailable} unavailable selection(s) have been excluded from your enquiry.</p>}<form onSubmit={prepare}><h3 className="basket-subheading">The little details</h3><label className="field">Your name<input required maxLength={80} autoComplete="given-name" value={details.name} onChange={e=>{setReview(false);setDetails({...details,name:e.target.value});}} placeholder="So we know who we’re baking for"/></label><fieldset className="fulfilment"><legend>How would you like your order?</legend>{(['collection','delivery'] as const).map(method=><label key={method} className={details.method===method?'selected':''}><input type="radio" name="method" value={method} checked={details.method===method} onChange={()=>{setReview(false);setDetails({...details,method});}}/>{method==='collection'?<MapPin size={19}/>:<Truck size={19}/>}<span><strong>{method==='collection'?'Self-collection':'Delivery'}</strong><small>{method==='collection'?'Pasir Ris · free':'Fee confirmed in chat'}</small></span></label>)}</fieldset><label className="field">Preferred {details.method==='collection'?'collection':'delivery'} date<input type="date" required min={singaporeToday()} value={details.date} onChange={e=>{setReview(false);setDetails({...details,date:e.target.value});}}/><small>We’ll check this date with Mama before confirming.</small></label>{details.method==='delivery'&&<label className="field">Delivery area or postal code<input required maxLength={200} value={details.address} onChange={e=>{setReview(false);setDetails({...details,address:e.target.value});}} placeholder="e.g. Tampines, 520123"/><small>This helps us work out the delivery fee.</small></label>}<label className="field">Anything else? <span className="optional">(optional)</span><textarea rows={3} maxLength={800} value={details.notes} onChange={e=>{setReview(false);setDetails({...details,notes:e.target.value});}} placeholder="Preferred timing, a special occasion, or questions about ingredients…"/></label><div className="totals"><div><span>Items subtotal</span><strong>{money(subtotal)}</strong></div><div><span>{details.method==='collection'?'Self-collection':'Delivery'}</span><span>{details.method==='collection'?'Free':'To be confirmed'}</span></div><small>{details.method==='delivery'?'Delivery is not included in the subtotal. We’ll confirm the final amount before payment.':'Payment is arranged in chat after we confirm your order.'}</small></div>{error&&<p role="alert" className="notice error">{error}</p>}{!review?<button className="button full" disabled={busy}>{busy?'Checking the latest menu…':'Prepare my WhatsApp message'}<ArrowRight size={18}/></button>:<div className="message-review"><h3>Your message is ready.</h3><p>Check it below, then open WhatsApp and tap send. Your order is confirmed only after we reply.</p><details><summary>Preview order message <ChevronDown size={16}/></summary><pre>{message}</pre></details><a className="button full whatsapp-button" href={whatsappLink(message)} target="_blank" rel="noreferrer"><MessageCircle size={18}/> Open WhatsApp <ArrowUpRight size={18}/></a><button type="button" className="copy-button" onClick={async()=>{try{await navigator.clipboard.writeText(message);setCopied(true);}catch{setError('Copy isn’t available in this browser. Open the preview above to select your message.');}}}>{copied?<Check size={15}/>:<Copy size={15}/>} {copied?'Copied!':'Copy message instead'}</button></div>}<p className="privacy-note">Your details stay in this page until you choose to open WhatsApp. No payment is taken here.</p></form></>}</div></Modal>;
}
