# A dedicated Neon/Postgres database.
DATABASE_URL=
# Only these verified Google emails may enter the admin or mutate the menu.
ADMIN_EMAILS=bryanongwenxi@gmail.com
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
# Generate with node -e "console.log(require('crypto').randomBytes(48).toString('hex'))"
SESSION_SECRET=
# Must exactly match the origin registered in your Google OAuth client.
NEXT_PUBLIC_SITE_URL=https://mamaongbakes.vercel.app
